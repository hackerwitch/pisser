// GET A BATHROOM CODE CLIENTSIDE CODE

// get the button and store a reference to it. (put it in a variable)

// listen for a click event (set up an event handler)

// ask permission to access geolocation (lat, lon)

// handle the success case, and the error case(no u can't have it, fuck off)

// SUCCESS: load map with the closest bathrooms
// - POST request to our server
// - handle our response (a list of locations)
// - render the map with our closest locations

// ERROR: show message, "Look for the nearest starbucks"

// SUBMIT A BATHROOM CODE

// get the button and store a reference to it. (put it in a variable)

// listen for a click event (set up an event handler)

// prompt the user to either a) submit geolocation or b) lookup and
// submit address.

// fill out the rest of the form.
// - tips: must buy? needles? accessibility? Code?
// - gendered? multiple or personal? comments? date.
// - name?
// submit to our server.

// wait for server response. SUCCESS or ERROR.

// SUCCESS: re-render map. Show success message.

// Error: Show error message.

